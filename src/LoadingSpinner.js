import React from 'react';

const LoadingSpinner = () => (
  <div className="loading-skeleton" style={{ height: '150px', width: '100%', margin: '20px 0' }}>
    Loading...
  </div>
);

export default LoadingSpinner;
